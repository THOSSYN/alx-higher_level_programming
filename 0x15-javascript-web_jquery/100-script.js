// Updates the color of the header

document.addEventListener('DOMContentLoaded', function () {
  const myHeader = document.querySelector('header');

  if (myHeader) {
    myHeader.style.color = 'red';
  }
});
